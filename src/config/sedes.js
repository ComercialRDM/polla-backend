// Sedes físicas donde se puede redimir un bono. Lista cerrada a propósito
// (selector, no texto libre) para que los reportes por sede sean confiables.
const SEDES = ['Aranjuez', 'Viva', 'Buenavista'];

module.exports = { SEDES };
